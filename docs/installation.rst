============
Installation
============

At the command line::

    $ easy_install cp_sqlalchemy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cp_sqlalchemy
    $ pip install cp_sqlalchemy